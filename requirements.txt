# FarmFix — COMP 368 Database Systems, Project 1
#
# One runtime dependency. SQLite3 is NOT listed, because the `sqlite3` module
# is part of the Python standard library — there is nothing to install and no
# database server to run.
#
# Install with:  pip install -r requirements.txt

Flask==3.1.3

# Testing only (the application runs without it):
pytest==9.1.1
